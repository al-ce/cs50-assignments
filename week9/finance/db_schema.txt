CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, username TEXT NOT NULL, hash TEXT NOT NULL, cash NUMERIC NOT NULL DEFAUL
T 10000.00);

CREATE TABLE sqlite_sequence(name,seq);

CREATE UNIQUE INDEX username ON users (username);


CREATE TABLE history (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, symbol INTEGER NOT NULL, price INTEGER NOT NULL, action TEXT NOT NULL, date INTEGER NOT NULL, time INTEGER NOT NULL, user_id TEXT NOT NULL);
